# Live Sports Scores 1.2 preview

API-Football v3 provider candidate. The API key is supplied by the installer
through Weynear's credential manager and is never stored in this repository or
passed to template code.

Promotion requires a newly built, signed and attested runtime artifact.
